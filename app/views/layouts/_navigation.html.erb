<nav id="top-nav" class="navbar navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand">
        <%= link_to image_tag("logo_white.png", class: "logo-brand" ) + "SafetyAlert", root_path %>
      </div>
    </div>

    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.full_name %> <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to "แก้ไขข้อมูล", edit_user_registration_path %></li>
              <li><%= link_to "ข้อมูลเตือนภัยพื้นที่ของคุณ", events_postalcode_path(current_user.location.postal_code) %></li>
              <li><%= link_to "ออกจากระบบ", destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "สมัคร", new_user_registration_path, class: "" %></li>
          <li><%= link_to "เข้าสู่ระบบ", new_user_session_path, class: "" %></li>
        <% end %>
      </ul>
    </div>

  </div>
</nav>
