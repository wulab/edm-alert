<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <meta property="og:image" content="https://cloud.githubusercontent.com/assets/403010/23497906/d4d11b90-ff57-11e6-89f9-4b8f5c0dc85f.png"/>
    <%= favicon_link_tag 'favicon.ico' %>
    <%= csrf_meta_tags %>
    <%= content_for :meta %>

    <title><%= content_for?(:title) ? [ yield(:title),  ' | EDM Alert'].join(' ') : 'EDM Alert' %></title>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="//maps.google.com/maps/api/js?key=AIzaSyB3qtDLQucaPCgDuqytET0hN7P6LuSLoDA"></script>
    <script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
    <script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script> <!-- only if you need custom infoboxes -->
  </head>

  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <div id="main" class="<%= "#{params[:controller].parameterize}-#{params[:action]}" %>">
      <div class="container">
        <%= render "layouts/alert" %>
        <%= yield %>
      </div>
    </div>

  </body>

  <script type="text/javascript">
    if ($("#map_canvas")[0]){
      handler = Gmaps.build('Google', { markers: { clusterer: undefined  }});
      handler.buildMap({
          provider: {
            disableDefaultUI: true,
            minZoom: 3,
            scaleControl: true,
            zoomControl: true
          },
          internal: {
            id: 'map_canvas'
          }
        },
        function(){
          var json_array = <%=raw @hash.to_json %>
          if (json_array.length > 0) {
            var markers = handler.addMarkers(json_array);
            handler.bounds.extendWith(markers);
            handler.fitMapToBounds();

            var i = 0;
            setInterval(function () {
              if (i == markers.length) i = 0;
              google.maps.event.trigger(markers[i].getServiceObject(), 'click');
              i++;
            }, 3000);
          }
          else {
            alert("ไม่มีข้อมูลเตือนภัยใน 7 วันที่ผ่านมา");
          }
        }
      );
    }
  </script>
</html>
